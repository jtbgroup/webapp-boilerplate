services:
  # Application (Backend + Frontend + Nginx)
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webappboilerplate-app
    ports:
      - "8090:8090"
    environment:
      # Profile: h2 (embedded) ou postgres (external)
      SPRING_PROFILES_ACTIVE: ${DB_PROFILE:-h2}
      SERVER_PORT: 8090
      
      # PostgreSQL config (utilisé si SPRING_PROFILES_ACTIVE=postgres)
      DB_HOST: ${DB_HOST:-postgres}
      DB_PORT: ${DB_PORT:-5432}
      DB_NAME: ${DB_NAME:-webappboilerplate}
      DB_USER: ${DB_USER:-webappboilerplate}
      DB_PASSWORD: ${DB_PASSWORD:-webappboilerplate}
      
      # JVM tuning
      JAVA_OPTS: "-Xmx1024m -Xms512m"
    
    depends_on:
      - postgres
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    restart: unless-stopped
    networks:
      - webappboilerplate-network

  # PostgreSQL (optionnel - utilisé si DB_PROFILE=postgres)
  postgres:
    image: postgres:17-alpine
    container_name: webappboilerplate-postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-webappboilerplate}
      POSTGRES_USER: ${DB_USER:-webappboilerplate}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-webappboilerplate}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-webappboilerplate}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - webappboilerplate-network
    profiles:
      - postgres

volumes:
  postgres_data:
    driver: local

networks:
  webappboilerplate-network:
    driver: bridge
