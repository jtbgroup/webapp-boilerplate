// Development environment — used when running: ng serve
// appVersion is updated automatically by: make set-version V=x.y.z
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080',
  appVersion: '1.0.0'
};