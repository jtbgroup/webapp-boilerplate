// Production environment — used automatically when building with: ng build --configuration production
// appVersion is updated automatically by: make set-version V=x.y.z
export const environment = {
  production: true,
  apiBaseUrl: '',
  appVersion: '1.0.0'
};