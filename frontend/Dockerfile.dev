# Frontend dev — hot reload via ng serve
FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . ./
CMD ["npx", "ng", "serve", "--host", "0.0.0.0", "--port", "4300", "--poll", "2000"]
