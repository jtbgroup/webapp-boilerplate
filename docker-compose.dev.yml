services:

  db:
    image: postgres:17
    environment:
      POSTGRES_DB: webappboilerplate
      POSTGRES_USER: webappboilerplate
      POSTGRES_PASSWORD: webappboilerplate
    ports:
      - "5432:5432"
    volumes:
      - pg_data_dev:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U webappboilerplate"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    ports:
      - "8080:8080"
      - "5005:5005"
    environment:
      DB_URL: jdbc:postgresql://db:5432/webappboilerplate
      DB_USERNAME: webappboilerplate
      DB_PASSWORD: webappboilerplate
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - maven_cache:/root/.m2

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "4300:4300"
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend

volumes:
  pg_data_dev:
  maven_cache:
